import { makeStyles, createStyles } from '@material-ui/core';

export const useStyles = makeStyles(theme =>
  createStyles({
    barsWrapper: {
      height: 'auto',
      margin: '0 auto',
    },
  })
);